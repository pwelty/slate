# Slate Dashboard Python Dependencies
# Core dependencies for dashboard functionality

# Template engine for rendering dashboard HTML
jinja2==3.1.6

# YAML parsing for configuration and widget definitions  
pyyaml==6.0.2

# HTTP requests for widget API integrations
requests==2.32.4

# RSS feed parsing for RSS widget
feedparser==6.0.11

# Optional development dependencies
watchdog==6.0.0      # File watching for auto-rebuild functionality